[project]
name = "meta_mpnn"
version = "0.1.0"
description = "Meta-MPNN experimental suite"
requires-python = ">=3.8"
authors = [
  { name = "Researcher" }
]

dependencies = [
  "torch>=2.0",
  "torch_geometric>=2.4",
  "matplotlib>=3.4",
  "pyyaml>=6.0",
  "numpy>=1.20"
]
