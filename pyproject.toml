[build-system]
requires = ["setuptools>=42", "wheel"]

[project]
name = "grade_gnn_research"
version = "0.1.0"
description = "Refactored experimental benchmark for GRADE-GNN vs baseline GCN"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
  {name = "Anonymous Researcher"}
]

dependencies = [
  # Pin NumPy to <2 to avoid binary-compat issues with PyTorch/CuPy wheels
  "numpy<2",
  # Panda 2.x requires NumPy>=2, so we pin pandas to <2 as it is not a critical dependency here
  "pandas<2",
  "torch==2.2.0",
  "torch-geometric",
  "ogb",
  "matplotlib",
  "pyyaml",
]

[tool.setuptools.packages.find]
where = ["src"]
