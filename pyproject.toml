[build-system]
requires = ["setuptools>=42", "wheel"]

[project]
name = "meta-mpnn"
version = "0.1.0"
description = "Research code – Meta-MPNN experiments"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Researcher", email = "research@example.com"}]

dependencies = [
  # Use vanilla PyTorch wheels; CPU wheels are selected automatically on platforms
  "torch==2.1.2",
  "torch_geometric==2.5.3",
  "torch-scatter==2.1.2",
  "pyyaml",
  "matplotlib",
  "numpy"
]
